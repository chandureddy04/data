package com.velsol.mynotes.data

import androidx.room.Entity
import androidx.room.PrimaryKey

@Entity
data class Note(val tile: String, val myNote: String) {

    @PrimaryKey(autoGenerate = true)
    var id: Int = 0
}
